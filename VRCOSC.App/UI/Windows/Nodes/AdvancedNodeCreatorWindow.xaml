<Window x:Class="VRCOSC.App.UI.Windows.Nodes.AdvancedNodeCreatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="AdvancedNodeCreatorWindow" Height="800" Width="450"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent">
    <Border Background="{DynamicResource CBackground1}" CornerRadius="10">
        <Grid Margin="10" VerticalAlignment="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBox Grid.Row="0" x:Name="TypeTextBox" Height="30" FontSize="20" TextChanged="TypeTextBox_OnTextChanged" KeyDown="TypeTextBox_OnKeyDown"/>
            <TextBlock Grid.Row="2" x:Name="FormedTypeText" Foreground="{DynamicResource CForeground2}" FontSize="18" FontWeight="SemiBold"/>
            <TextBlock Grid.Row="4" x:Name="SuggestedTypes" Foreground="{DynamicResource CForeground2}" FontSize="18" FontWeight="SemiBold"/>
        </Grid>
    </Border>
</Window>