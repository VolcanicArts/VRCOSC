<Window x:Class="VRCOSC.App.UI.Windows.Nodes.NodeCreatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:core="clr-namespace:VRCOSC.App.UI.Core"
        mc:Ignorable="d"
        Height="800" Width="450"
        Background="{DynamicResource CBackground2}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <core:SpacedStackPanel Grid.Row="0" Spacing="10">
            <TextBlock Text="Generic Arguments:" Foreground="{DynamicResource CForeground1}" FontSize="20" FontWeight="SemiBold"/>
            <TextBox x:Name="GenericArgumentText" Height="30" FontSize="20" TextChanged="GenericArgumentText_OnTextChanged"/>
            <TextBlock x:Name="FormedTypeText" Foreground="{DynamicResource CForeground2}" FontSize="18" FontWeight="SemiBold"/>
        </core:SpacedStackPanel>
        <core:TextButton Grid.Row="2" x:Name="CreateNodeButton" Text="Create Node" ButtonColour="Blue" Height="25" FontSize="18" VerticalAlignment="Top" Foreground="{DynamicResource CForeground1}"
                         Click="CreateNodeButton_OnClick"/>
        <ItemsControl Grid.Row="4"
            ItemsSource="{Binding CommonTypesSource}"
                      VerticalAlignment="Top" HorizontalAlignment="Stretch">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <core:SpacedStackPanel Spacing="10" VerticalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock FontSize="15" TextAlignment="Left" Text="{Binding Key}" Foreground="{DynamicResource CForeground1}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>