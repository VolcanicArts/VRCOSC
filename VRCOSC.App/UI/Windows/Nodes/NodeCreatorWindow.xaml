<Window x:Class="VRCOSC.App.UI.Windows.Nodes.NodeCreatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:core="clr-namespace:VRCOSC.App.UI.Core"
        mc:Ignorable="d"
        Height="165"
        Width="450"
        Background="{DynamicResource CBackground2}"
        ResizeMode="NoResize">
    <Grid Margin="10" VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" x:Name="TitleText" Foreground="{DynamicResource CForeground1}" FontSize="20" FontWeight="SemiBold"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="GenericArgumentText" Height="30" FontSize="20" TextChanged="GenericArgumentText_OnTextChanged" KeyDown="GenericArgumentText_OnKeyDown"/>
        <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"  x:Name="FormedTypeText" Foreground="{DynamicResource CForeground2}" FontSize="18" FontWeight="SemiBold"/>
        <core:TextButton Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" x:Name="CreateNodeButton" Text="Create Node" ButtonColour="Blue" Height="25" FontSize="18" VerticalAlignment="Top" Foreground="{DynamicResource CForeground1}"
                         Click="CreateNodeButton_OnClick"/>
    </Grid>
</Window>