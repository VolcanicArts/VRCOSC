<UserControl x:Class="VRCOSC.App.UI.Core.KeybindPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="Transparent"
             Focusable="True"
             FocusVisualStyle="{x:Null}"
             d:DesignHeight="300" d:DesignWidth="300"
             MouseDown="KeybindPicker_OnMouseDown"
             PreviewKeyDown="KeybindPicker_OnPreviewKeyDown">
    <Border Background="{StaticResource CBackground1}">
        <TextBlock Text="{Binding StringRepresentation}" Foreground="{StaticResource CForeground1}" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Border>
</UserControl>
