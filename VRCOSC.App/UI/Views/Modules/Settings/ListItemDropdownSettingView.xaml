<UserControl x:Class="VRCOSC.App.UI.Views.Modules.Settings.ListItemDropdownSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
        <ComboBox Height="30" ItemsSource="{Binding Items}" SelectedValue="{Binding Attribute.Value}"
                  DisplayMemberPath="{Binding TitlePath}" SelectedValuePath="{Binding ValuePath}"
                  HorizontalAlignment="Left"
                  FontSize="16" FontWeight="Regular" Width="200"/>
</UserControl>