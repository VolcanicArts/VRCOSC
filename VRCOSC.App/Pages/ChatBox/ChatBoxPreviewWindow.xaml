<Window x:Class="VRCOSC.App.Pages.ChatBox.ChatBoxPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="ChatBox Preview" Height="450" Width="800" MinHeight="450" MinWidth="800">
    <Grid>
        <Border Background="{StaticResource CBackground1}" Padding="10">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Border Name="ChatBoxContainer" Padding="30 10" CornerRadius="15" Background="#27272c" Visibility="Collapsed">
                    <TextBlock Name="ChatBoxText" FontSize="30" Foreground="{StaticResource CForeground1}"
                               TextAlignment="Center" TextWrapping="Wrap" MaxWidth="435" />
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
