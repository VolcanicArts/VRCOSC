<Page x:Class="VRCOSC.App.Pages.Modules.Settings.SliderSettingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:VRCOSC.App.Pages.Modules.Settings"
      mc:Ignorable="d"
      Title="FloatSliderSettingPage"
      Width="{Binding ActualWidth, ElementName=SettingPageFrame}">
    <Grid Height="50">
        <Slider Value="{Binding Attribute.Value}" Minimum="{Binding MinValue, Mode=OneTime}" Maximum="{Binding MaxValue, Mode=OneTime}" TickFrequency="{Binding TickFrequency, Mode=OneTime}" TickPlacement="BottomRight" IsSnapToTickEnabled="True"/>
        <TextBlock Text="{Binding MinValue, Mode=OneTime}" HorizontalAlignment="Left" VerticalAlignment="Bottom" Foreground="{StaticResource CForeground1}" FontSize="20"/>
        <TextBlock Text="{Binding Attribute.Value, Mode=OneWay, StringFormat='F2'}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Foreground="{StaticResource CForeground1}" FontSize="20"/>
        <TextBlock Text="{Binding MaxValue, Mode=OneTime}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Foreground="{StaticResource CForeground1}" FontSize="20"/>
    </Grid>
</Page>
