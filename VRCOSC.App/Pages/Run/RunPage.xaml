<Page x:Class="VRCOSC.App.Pages.Run.RunPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="clr-namespace:VRCOSC.App.UI"
      xmlns:fa5="http://schemas.fontawesome.com/icons/"
      mc:Ignorable="d">
    <Grid>
        <Border Background="{StaticResource CBackground2}" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="7*" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="{StaticResource CBackground5}" HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch" Padding="5" CornerRadius="5">
                    <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Orientation="Vertical" />
                </Border>
                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="60" />
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="{StaticResource CBackground5}" HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch" Padding="5" CornerRadius="5">
                        <TextBlock Text="Hello" Foreground="White"/>
                    </Border>
                    <Border Grid.Row="2" Background="{StaticResource CBackground5}" HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch" Padding="10" CornerRadius="5">
                        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <ui:SpacedStackPanel FlowDirection="LeftToRight" Orientation="Horizontal" Spacing="10">
                                <Button Style="{StaticResource VRCOSCGreenButtonStyle}" Width="150" VerticalAlignment="Stretch">
                                    <fa5:ImageAwesome Icon="Solid_Play" Foreground="{StaticResource CForeground1}" Width="23"/>
                                </Button>
                            </ui:SpacedStackPanel>
                            <ui:SpacedStackPanel FlowDirection="RightToLeft" Orientation="Horizontal" Spacing="10">
                                <Button Style="{StaticResource VRCOSCRedButtonStyle}" Width="150" VerticalAlignment="Stretch">
                                    <fa5:ImageAwesome Icon="Solid_Stop" Foreground="{StaticResource CForeground1}" Width="23"/>
                                </Button>
                                <Button Style="{StaticResource VRCOSCBlueButtonStyle}" Width="150" VerticalAlignment="Stretch">
                                    <fa5:ImageAwesome Icon="Solid_Redo" Foreground="{StaticResource CForeground1}" Width="23"/>
                                </Button>
                            </ui:SpacedStackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </Border>

    </Grid>
</Page>
